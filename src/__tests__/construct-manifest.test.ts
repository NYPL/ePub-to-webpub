/**
 * - extracts plain title
 * - falls back to DCMetadata
 * - extracts only main title when there are many
 */
describe('extract title', () => {
  it('extracts a main title fiels', () => {
    throw new Error();
  });
});
